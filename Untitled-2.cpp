#include <stdio.h>
int a,b,sum,mul;
int main(){
    printf("nhap 2 so la a,b :");
    scanf("%d%d",&a,&b);
    sum = a+b;
    mul = a*b;
    printf("tong 2 so la %d", sum);
    printf("/ntich 2 so la %d", mul);
    return 0 ;

}